stages:
  - build

build_api:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login registry.gitlab.com -u $REGISTRY_USERNAME -p $REGISTRY_PASSWORD
  script:
    - docker build -t registry.gitlab.com/api:$CI_COMMIT_REF_SLUG -f api/docker/Dockerfile api/
    - docker push registry.gitlab.com/api:$CI_COMMIT_REF_SLUG